<div id="modal-phrases" class="ui small modal">
  <div class="header"><%= __('Add Phrase') %> </div>
  <div class="content">
    <div class="ui form">
      <div class="field">
        <label><%= __('Ruso') %> </label>
        <input type="text" id="phrase_ru" placeholder="<%= __('Write a russian phrase') %>">
      </div>
      <div class="field">
        <label><%= __('English') %> </label>
        <input type="text" id="phrase_en" placeholder="<%= __('Write a english phrase') %>">
      </div>
      <div class="field">
        <label><%= __('Spanish') %> </label>
        <input type="text" id="phrase_es" placeholder="<%= __('Write a spanish phrase') %>">
      </div>
      <div class="field">
        <label><%= __('Source') %> </label>
        <div id="source" class="ui selection dropdown">
          <input type="hidden">
          <i class="dropdown icon"></i>
          <div class="default text">Seleccione</div>
          <div class="menu">
            <% _.each(sources, function (source){ %>
              <div class="item" data-value="<%= source.id %>"><%= source.name %></div>
            <% }) %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="actions">
    <div class="ui approve green button"><%= __('Approve') %> </div>
    <div class="ui cancel red button"><%= __('Cancel') %> </div>
  </div>
</div>
